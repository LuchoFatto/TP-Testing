# Segundo Sprint Testers

#### **Escenarios**

| Escenario                                                                                                         | Descripción                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Incremento o reducción válida. **(Happy Path)**                                                                   | Dado que existe un evento con ID 123, cuya capacidad actual es 100 y tiene 40 entradas vendidas. Y estoy autenticado con un token válido.} Cuando envío una petición PATCH /events/123/capacity con una nueva capacidad de 80. Entonces el sistema debe actualizar la capacidad en el archivo JSON. Y debe responder con un código de estado 200 OK junto con el evento modificado.                                                          |
| **Error de Negocio** - Reducción por debajo de lo vendido (Caso de Borde)                                         | Dado que existe un evento con ID 123, con 60 entradas ya vendidas. Cuando intento modificar la capacidad a 50. Entonces el sistema NO debe aplicar el cambio. Y debe responder un código de estado 400 Bad Request con el mensaje: { "detail": "La nueva capacidad no puede ser menor a las entradas vendidas." }                                                                                                                            |
| **Error de Validación** - Capacidad igual o menor a cero                                                          | Cuando intento modificar la capacidad de un evento a 0 o a un número negativo (ej -10). Entonces el sistema debe rechazar la solicitud con un código de estado 400 Bad Request o 422 Unprocessable Entity.                                                                                                                                                                                                                                   |
| **Error de Ciclo de Vida** - Evento Eliminado Lógicamente                                                         | Dado que el evento con ID 123 fue marcado previamente como eliminado (deleted: true o active: false según corresponda). Cuando intento modificar su capacidad. Entonces el sistema debe denegar la acción y responder con un 400 Bad Request indicando que el evento está inactivo o borrado.                                                                                                                                                |
| **Error de Autorización** - Usuario común intenta modificar un evento ajeno                                       | Dado que existe un evento con ID 3 cuyo owner_username es "organizer1". Y estoy autenticado como el usuario "organizer2" (rol usuario común). Cuando envío una petición PATCH /events/3/capacity con una nueva capacidad de 60. Entonces el sistema NO debe aplicar el cambio. Y debe responder un código de estado 403 Forbidden con el mensaje: { "detail": "No tiene permisos para modificar este evento." }                              |
| **Administrador elimina cualquier evento (Happy Path)** - El administrador es capaz de eliminar cualquier evento. | Dado que existe un evento con ID 3 y tiene órdenes registradas en orders.json.<br/>Y estoy autenticado con un token de administrador (token-admin1).<br/>Cuando envío una petición DELETE /eventos/3.<br/>Entonces el sistema debe cambiar el atributo deleted a true en el archivo JSON. Y debe responder con un código de estado 200 OK (o 204 No Content).                                                                                |
| Dueño elimina evento SIN órdenes - **(Happy Path)**                                                               | Al no haber órdenes en un evento el dueño es capaz de borrarlo. Dado que existe un evento con ID 5 cuyo owner_username es "organizer1".<br/>Y el evento NO tiene ninguna orden asociada en orders.json (ni activas ni canceladas). Ademas estoy autenticado como "organizer1".<br/>Cuando envío una petición DELETE /events/5 el sistema debe realizar el borrado lógico del evento.<br/>Y debe responder con un código de estado 200 OK.    |
| **Error de Negocio** - Dueño intenta eliminar evento con órdenes históricas                                       | Dado que existe un evento con ID 3 cuyo owner_username es "organizer1".<br/>Y el evento tiene al menos una orden en orders.json con estado "cancelled". Y estoy autenticado como "organizer1".                                                                                                                                                                                                                                               |
| **Error de Autorización** - Organizador                                                                           | Dado que existe un evento con ID 3 cuyo no dueño intenta eliminar owner_username es "organizer1". Y estoy autenticado como el usuario "organizer2" (rol usuario común). Cuando envío una petición DELETE /events/3. Entonces el sistema debe denegar la acción. Y debe responder un código de estado 403 Forbidden con el mensaje: { "detail": "No tiene permisos para eliminar este evento." }                                              |
| **Error de Existencia** - ID no encontrado                                                                        | Cuando un usuario (Admin o no) envía una petición DELETE /events/9999 (ID inexistente). Entonces el sistema debe responder un código de estado 404 Not Found con el mensaje: { "detail": "Evento no encontrado." }                                                                                                                                                                                                                           |
| **(Happy Path)** - Registro exitoso de un nuevo cliente                                                           | Dado que el username "nuevo_cliente" no existe dentro del archivo users.json, cuando envío una petición POST /auth/register con los datos obligatorios del esquema y el rol "customer", entonces el sistema debe añadir el nuevo registro de usuario en el archivo JSON y debe responder con un código de estado 200 OK confirmando la creación.                                                                                             |
| **Error de Negocio** - Intento de registro con un usuario ya existente                                            | Si intentamos registrar el username "admin1" mediante una petición POST /auth/register, pero este identificador ya se encuentra guardado en el archivo users.json, el sistema tiene que rechazar la solicitud respondiendo con un código de estado 400 Bad Request que indique que el usuario ya existe, garantizando así que no se altere de ninguna forma el archivo de persistencia.                                                      |
| **Camino Feliz (Happy Path)** - Inicio de sesión correcto                                                         | Al enviar una petición POST /auth/login con las credenciales correctas del usuario "organizer1", el sistema tiene que validar estos datos contra el archivo users.json y, tras confirmar la coincidencia estricta, responder con un código de estado 200 OK que incluya la estructura del access_token.                                                                                                                                      |
| **Error de Autenticación** - Intento de login con credenciales inválidas                                          | Si se envía una petición POST /auth/login para el usuario existente "organizer1" pero la contraseña proporcionada es incorrecta, el sistema debe interrumpir inmediatamente el proceso de autenticación y responder con un código de estado 401 Unauthorized, denegando así la entrega del token.                                                                                                                                            |
| **Camino Feliz (Happy Path)** - Creación exitosa de un evento                                                     | Cuando un usuario autenticado como "organizer1" envía una petición POST /events con datos válidos (nombre, fecha futura, capacidad entera mayor a cero y precio positivo), el sistema debe registrar el evento en events.json vinculando de forma automática el "owner_username": "organizer1", estableciendo los valores iniciales por defecto y respondiendo con un código de estado 200 OK junto con la entidad del evento recién creada. |
| **Error de Validación** - Datos inconsistentes según esquema estructural                                          | Si se envía una petición POST /events con un token de acceso válido en los headers, pero el valor de la capacidad no cumple con el modelo (por ser un número decimal o negativo), la capa de validación debe interceptar y frenar la solicitud de forma reactiva antes de que llegue a la lógica del servicio, respondiendo inmediatamente con un código de estado 422 Unprocessable Content.                                                |
| Consulta exitosa de eventos activos - **(Happy Path)**<br>                                                        | Dado que existen múltiples eventos activos registrados en el archivo `events.json`. Cuando envío una petición `GET /events`. Entonces el sistema debe recuperar los eventos disponibles. Y debe responder con un código de estado **200 OK** devolviendo una colección de eventos.                                                                                                                                                           |
| Filtro de Negocio - **Eventos eliminados lógicamente no visibles**                                                | Dado que existen eventos marcados con `deleted: true`. Cuando realizo una consulta `GET /events`. Entonces el sistema no debe incluir dichos eventos en la respuesta. Y debe responder con un código de estado **200 OK**.                                                                                                                                                                                                                   |
| **Caso de Borde** - No existen eventos activos<br>                                                                | Dado que no existen eventos activos registrados. Cuando envío una petición `GET /events`. Entonces el sistema debe responder correctamente. Y debe devolver una lista vacía junto con un código de estado **200 OK**.                                                                                                                                                                                                                        |
| Consulta exitosa - **(Happy Path)**                                                                               | Dado que existe un evento con ID 123. Cuando envío una petición `GET /events/123`. Entonces el sistema debe recuperar los datos completos del evento. Y debe responder con un código de estado **200 OK** devolviendo la entidad correspondiente.                                                                                                                                                                                            |
| Error de Existencia - **Evento inexistente**                                                                      | Cuando envío una petición `GET /events/9999`. Entonces el sistema debe responder con un código de estado **404 Not Found**. Y debe devolver el mensaje: `{"detail": "Event not found"}`.                                                                                                                                                                                                                                                     |
| **Error de Ciclo de Vida** - Evento eliminado lógicamente<br>                                                     | Dado que existe un evento con ID 123 marcado como eliminado. Cuando intento consultar su detalle. Entonces el sistema debe denegar la operación. Y debe responder con un código de estado **404 Not Found**.                                                                                                                                                                                                                                 |
| Actualización exitosa - **(Happy Path)**                                                                          | Dado que existe un evento con ID 123. Y estoy autenticado como dueño del evento o administrador. Cuando envío una petición `PUT /events/123` con datos válidos. Entonces el sistema debe actualizar la información en `events.json`. Y debe responder con un código de estado **200 OK** devolviendo la entidad actualizada.                                                                                                                 |
| **Error de Autorización** - Usuario sin permisos                                                                  | Dado que el evento pertenece a otro organizador. Cuando intento modificarlo. Entonces el sistema debe rechazar la solicitud. Y debe responder con un código de estado **403 Forbidden**.                                                                                                                                                                                                                                                     |
| **Error de Validación** - Datos inválidos                                                                         | Cuando envío un nombre vacío, un precio negativo o una capacidad inválida. Entonces el sistema debe rechazar la solicitud. Y debe responder con un código de estado **422 Unprocessable Content**.                                                                                                                                                                                                                                           |
| **Error de Existencia** - Evento inexistente                                                                      | Cuando intento modificar el evento con ID 9999. Entonces el sistema debe responder con un código de estado **404 Not Found**.                                                                                                                                                                                                                                                                                                                |
| Desactivación exitosa -  **(Happy Path)**                                                                         | Dado que existe un evento activo con ID 123. Y estoy autenticado como dueño o administrador. Cuando envío una petición `PATCH /events/123/deactivate`. Entonces el sistema debe marcar el evento como inactivo. Y debe responder con un código de estado **200 OK**.                                                                                                                                                                         |
| **Error de Autorización** - Usuario sin permisos                                                                  | Dado que el evento pertenece a otro organizador. Cuando intento desactivarlo. Entonces el sistema debe responder con un código de estado **403 Forbidden**.                                                                                                                                                                                                                                                                                  |
| **Error de Existencia** - Evento inexistente                                                                      | Cuando envío una petición `PATCH /events/9999/deactivate`. Entonces el sistema debe responder con un código de estado **404 Not Found**.                                                                                                                                                                                                                                                                                                     |
| **Caso de Borde** - Evento ya desactivado                                                                         | Dado que el evento ya se encuentra inactivo. Cuando intento desactivarlo nuevamente, entonces el sistema debe responder informando devolviendo el estado del evento que ya se encuentra desactivado y un código **200 OK**.                                                                                                                                                                                                                  |
| **Consulta exitosa** - (Happy Path)                                                                               | Dado que existen órdenes registradas. Y estoy autenticado correctamente. Cuando envío una petición `GET /orders`. Entonces el sistema debe devolver las órdenes permitidas según mi rol. Y debe responder con un código de estado **200 OK**.                                                                                                                                                                                                |
| Error de Autenticación - **Token inexistente**                                                                    | Cuando realizo una petición sin token válido. Entonces el sistema debe responder con un código de estado **401 Unauthorized**.                                                                                                                                                                                                                                                                                                               |
| **Caso de Borde** - Sin órdenes registradas                                                                       | Dado que no existen órdenes para el usuario. Cuando consulto `GET /orders`. Entonces el sistema debe responder con una lista vacía. Y debe devolver un código de estado **200 OK**.                                                                                                                                                                                                                                                          |
| **Compra exitosa** - (Happy Path)                                                                                 | Dado que existe un evento activo con disponibilidad suficiente. Y estoy autenticado como cliente. Cuando envío una petición `POST /orders` indicando una cantidad válida de entradas. Entonces el sistema debe registrar la orden en `orders.json`. Y debe responder con un código de estado **200 OK**.                                                                                                                                     |
| **Error de Negocio** - Capacidad insuficiente                                                                     | Dado que el evento no posee entradas disponibles suficientes. Cuando intento realizar la compra. Entonces el sistema debe rechazar la operación. Y debe responder con un código de estado **400 Bad Request**.                                                                                                                                                                                                                               |
| **Error de Ciclo de Vida** - Evento inactivo                                                                      | Dado que el evento se encuentra desactivado o eliminado. Cuando intento comprar entradas. Entonces el sistema debe responder con un código de estado **400 Bad Request**.                                                                                                                                                                                                                                                                    |
| **Error de Validación** - Cantidad inválida                                                                       | Cuando intento comprar 0 o una cantidad negativa de entradas. Entonces el sistema debe rechazar la solicitud. Y debe responder con un código de estado **422 Unprocessable Content**.                                                                                                                                                                                                                                                        |
| **Cancelación exitosa** -  (Happy Path)                                                                           | Dado que existe una orden activa con ID 10. Y estoy autenticado como propietario de la orden. Cuando envío una petición `PATCH /orders/10/cancel`. Entonces el sistema debe actualizar el estado de la orden a `"cancelled"`. Y debe responder con un código de estado **200 OK**.                                                                                                                                                           |
| **Error de Autorización** - Usuario ajeno a la orden                                                              | Dado que la orden pertenece a otro usuario. Cuando intento cancelarla. Entonces el sistema debe responder con un código de estado **403 Forbidden**.                                                                                                                                                                                                                                                                                         |
| **Error de Existencia** - Orden inexistente                                                                       | Cuando envío una petición `PATCH /orders/9999/cancel`. Entonces el sistema debe responder con un código de estado **404 Not Found**. Y debe devolver el mensaje: `{"detail": "Orden no encontrada."}`.                                                                                                                                                                                                                                       |
| **Caso de Borde** - Orden ya cancelada                                                                            | Dado que la orden ya posee estado `"cancelled"`. Cuando intento cancelarla nuevamente. Entonces el sistema debe rechazar la operación. Y debe responder con un código de estado **400 Bad Request**.                                                                                                                                                                                                                                         |

#### **Casos de Prueba**

Basándose en el documento de requerimientos refinados que el PM armó en el Sprint 1 se construyen los casos de prueba.

|    ID     |                                 Nombre                                  |                                                                                                              Precondiciones                                                                                                               |                                                                                                Datos de entrada                                                                                                 |                                                                                                                        Pasos                                                                                                                        |                                                                                                                                                                                     Resultado esperado                                                                                                                                                                                      |
| :-------: | :---------------------------------------------------------------------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: | :-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: |
| **CP_01** |             **Incremento o reducción de capacidad valida**              |                                                                   • Usuario autenticado o con Token Valido de dueño o administrador<br/>• Evento 123 existente y activo                                                                   |                                                 **event_id (Path):**<br/>123<br/>**Authorization (Header):** token-admin1 </br>**Body:**<br/>{ "capacity": 80 }                                                 |                \- Configurar una petición HTTP PATCH a /events/123/capacity. <br/>- Incluir el token de autenticación en los headers.<br/>\- Incluir en el cuerpo de la petición la nueva capacidad (80).<br/>\- Enviar la petición                 |                                                                                                             Código de estado 200.<br/>El sistema actualiza la capacidad en el archivo JSON a 80.<br/> La respuesta contiene el objeto del evento modificado con el nuevo valor.                                                                                                             |
| **CP_02** |       Reducción de capacidad por debajo de las entradas vendidas        |                                     • El evento 123 existe y tiene 60 entradas vendidas registradas.<br/>• El usuario está autenticado con permisos para modificar el evento o tiene un token válido.                                     |                                                                      **event_id (Path):** 123<br/>**Authorization (Header):** token-admin1                                                                      | - Configurar la petición HTTP PATCH hacia el endpoint /events/123/capacity.<br>- Agregar el token de autenticación en el Header.<br>- Incluir en el body el JSON con la nueva capacidad menor a las entradas vendidas (50).<br>- Enviar la petición |                                                                                                      Código de estado 400<br/>El sistema NO aplica ningún cambio en el archivo JSON.<br/>La respuesta indica que la nueva capacidad no puede ser menor al número de entradas vendidas.                                                                                                      |
| **CP_03** |           Intento de asignar capacidad igual o menor a cero.            |                                              • El usuario está autenticado con permisos o tiene un token válido.<br/>• El evento 123 existe y tiene cierta capacidad establecida mayor a 0.                                               |                                                     **event_id (Path):** 123<br/>**Authorization (Header):** token-admin1 <br/>**Body:** { "capacity": 0 }                                                      |     \- Configurar la petición HTTP PATCH hacia el endpoint /events/123/capacity.<br/>- Agregar el token de autenticación en el Header.<br/>- Ingresar en el cuerpo de la petición una capacidad menor o igual a cero.<br/>- Enviar la petición      |                                                                                                Código de estado 400 Bad Request<br/>El sistema rechaza la solicitud y la capacidad no se modifica en el archivo.<br/>La respuesta indica que la capacidad no puede ser menor o igual a cero.                                                                                                |
| **CP_04** | Rechazo de modificación de capacidad en un evento eliminado lógicamente |                                • Existe un evento con ID 123<br/>• El evento tiene los atributos "active": false y "deleted": true<br/>• El usuario está autenticado con permisos o tiene un token válido.                                |                                                 **event_id (Path):**<br/> 123 <br/>**Authorization (Header):** token-admin1 <br/>**Body:** { "capacity": 150 }                                                  |             \- Configurar la petición HTTP PATCH hacia el endpoint /events/123/capacity.<br/>- Agregar el token de autenticación en el Header.<br/>- Incluir en el Body el JSON con la nueva capacidad (150).<br/>- Enviar la petición.             |                                                                                                                       El sistema deniega la acción devolviendo un código de estado 400 Bad Request.<br/>La respuesta indica que no se ha encontrado un evento activo.                                                                                                                       |
| **CP_05** |     Rechazo de modificación de capacidad por usuario no autorizado      |                                      • Existe un evento con ID 3<br/>• El creador del evento es organizer1<br/>• Estar autenticado en el sistema como el usuario organizer2 o tener un token válido                                       |                                                  **event_id (Path):** 3<br/>**Authorization (Header):**<br/>token-organizer2 <br/>**Body:** { "capacity": 60 }                                                  |                  Configurar la petición HTTP PATCH hacia el endpoint /events/3/capacity<br/>- Agregar en el Header el token de organizer2.<br/>- Incluir en el Body el JSON con la nueva capacidad (60).<br/>- Enviar la petición.                  |                                                                                           El sistema no aplica ningún cambio.<br/>Se recibe un código de estado 403 Forbidden.<br/>El cuerpo de la respuesta indica que no se poseen permisos suficientes para modificar la capacidad del evento.                                                                                           |
| **CP_06** |       Eliminación lógica de un evento por parte del Administrador       |                  • Existe un evento con ID 3<br/>• El evento registra órdenes asociadas en el archivo orders.json.<br/>• Estar autenticado con un usuario de rol Administrador o tener un token válido de administrador                   |                                                                     **event_id (Path):**<br/>3<br/>**Authorization (Header):** token-admin1                                                                     |                                      \- Configurar la petición HTTP utilizando el método DELETE hacia el endpoint /events/3.<br/>- Agregar el token del administrador en el Header.<br/>- Enviar la petición.                                       |                                                                                                       El sistema responde con un código de estado 200 OK.<br/>Al revisar el archivo JSON, el evento con ID 3 ahora tiene su atributo de borrado lógico en verdadero (deleted: true).                                                                                                        |
| **CP_07** |           Eliminación lógica de un evento por parte del Dueño           | • Existe un evento con ID 5 cuyo campo owner_username es organizer1<br/>• El evento con ID 5 no posee ninguna orden registrada en el archivo orders.json.<br/>• Estar autenticado como el usuario organizer1 o tener el token del usuario |                                                                **event_id (Path):**</br>5<br/>**Authorization (Header):**<br/> token-organizer1                                                                 |                                                   \- Configurar la petición HTTP DELETE hacia el endpoint /events/5.<br/>- Agregar el token de organizer1 en el Header.<br/>- Enviar la petición.                                                   |                                                                                                           El sistema procesa la solicitud con éxito y devuelve un código de estado 200 OK.<br/>El evento cambia su estado interno a eliminado lógicamente en el archivo de datos.                                                                                                           |
| **CP_08** |     Eliminación por parte del dueño si el evento cuenta con órdenes     | • Existe un evento con ID 3<br/>• El evento le pertenece al usuario organizer1<br/>• El evento tiene al menos una orden registrada con estado “cancelled”<br/>• Estar autenticado como el usuario organizer1 o tener el token del usuario |                                                                **event_id (Path):**</br>3<br/>**Authorization (Header):**<br/> token-organizer1                                                                 |                                                   \- Configurar la petición HTTP DELETE hacia el endpoint /events/3.<br/>- Agregar el token de organizer1 en el Header.<br/>- Enviar la petición.                                                   |                                                            El sistema bloquea la acción y responde con un código de estado 400 Bad Request.<br/>El cuerpo de la respuesta indica que no se puede eliminar un evento que ya tiene órdenes registradas.<br/>El estado del evento en el JSON de persistencia permanece sin cambios.                                                            |
| **CP_09** |       Eliminación de evento por parte de un organizador no dueño        |                                • Existe un evento con ID 3<br/>• El evento le pertenece a organizer1<br/>• Estar autenticado en la aplicación con el usuario común organizer2 o tener el token del usuario                                |                                                                **event_id (Path):** </br>3<br/>**Authorization (Header):**<br/>token-organizer2                                                                 |                                                   \- Configurar la petición HTTP DELETE hacia el endpoint/events/3.<br/>- Agregar el token de organizer2 en el Header.<br/>- Enviar la petición.                                                    |                                                                                         El sistema deniega el borrado y responde con un código de estado 403 Forbidden.<br/>El cuerpo de la respuesta contiene el mensaje indica que no se poseen permisos suficientes para eliminar dicho evento.                                                                                          |
| **CP_10** |              Eliminación para un ID de evento inexistente               |                                       • El usuario se encuentra autenticado en el sistema (con cualquier rol válido).<br/>• El ID del evento propuesto no existe en los registros de la aplicación.                                       |                                                               **event_id (Path):**</br>9999<br/>**Authorization (Header):**<br/>token-organizer2                                                                |                                                \- Configurar la petición HTTP DELETE hacia el endpoint /events/9999.<br/>- Agregar el token de autenticación en el Header.<br/>- Enviar la petición.                                                |                                                                                                              El sistema determina que el recurso no existe y devuelve un código de estado 404 Not Found.<br/>El cuerpo de la respuesta indica que no se encontró dicho evento.                                                                                                              |
| **CP_11** |                  Registro exitoso de un nuevo cliente                   |                                                                • No existe ningún usuario con username "nuevo_cliente".<br />• El endpoint /auth/register está operativo.                                                                 |                                                                       **Body:**<br/>{"username": "nuevo_cliente","password": "sixSeven"}                                                                        |                                           \- Configurar una petición HTTP POST hacia el endpoint /auth/register.<br/>\- Incluir en el body los datos obligatorios del usuario.<br/>\- Enviar la petición.                                           |                                                                                         Código de estado 200 OK.<br/>El sistema agrega el nuevo usuario al archivo users.json.<br/>La respuesta contiene los datos del usuario registrado.<br/>El registro queda persistido con los datos enviados.                                                                                         |
| **CP_12** |             Intento de registro con un usuario ya existente             |                                                                  • Existe previamente un usuario con username "admin1".<br/>• El endpoint /auth/register está operativo.                                                                  |                                                                          **Body:**<br/>{"username": "admin1",  "password": "sixSeven"}                                                                          |                              - Configurar una petición HTTP POST hacia el endpoint /auth/register.<br />- Incluir en el body los datos obligatorios utilizando el username "admin1".<br /><br />- Enviar la petición.                               |                                                                           Código de estado 400 Bad Request.<br />La respuesta indica que el usuario ya existe.<br />El sistema no agrega ningún registro nuevo al archivo users.json.<br />El contenido del archivo de persistencia permanece sin modificaciones.                                                                           |
| **CP_13** |                        Inicio de sesión correcto                        |                                                  • Existe un usuario con username "organizer1" registrado en el archivo users.json.<br/> • La contraseña asociada al usuario es válida.                                                   |                                                                        **Body:**<br/>{"username": "organizer1",  "password": "sixSeven"}                                                                        |                                           - Configurar una petición HTTP POST hacia el endpoint /auth/login.<br />- Incluir en el body las credenciales correctas del usuario.<br />- Enviar la petición.                                           |                                                                                                                 Código de estado 200 OK.<br/>El sistema valida las credenciales contra el archivo users.json.<br/>La respuesta contiene una estructura access_token válida.                                                                                                                 |
| **CP_14** |               Intento de login con credenciales inválidas               |                                     • Existe un usuario con username "organizer1" registrado en el archivo users.json.<br/> • La contraseña enviada no coincide con la almacenada para dicho usuario.                                     |                                                                   **Body:**<br/>{"username": "organizer1","password": "password_incorrecta"}                                                                    |                                    - Configurar una petición HTTP POST hacia el endpoint /auth/login.<br />- Incluir en el body el username "organizer1" y una contraseña incorrecta.<br />- Enviar la petición.                                    |                                                                 Código de estado 401 Unauthorized.<br />El sistema rechaza la autenticación al detectar que las credenciales no son válidas.<br />La respuesta indica que el usuario o la contraseña son incorrectos.<br />No se genera ni se devuelve ningún access_token.                                                                 |
| **CP_15** |                      Creación exitosa de un evento                      |                                • Existe un usuario "organizer1" registrado en el sistema.<br/> • El usuario se encuentra autenticado mediante un token válido.<br/> • El endpoint /events está operativo.                                 |                              **Authorization (Header):**<br/>token-organizer1<br />**Body:**<br/>{"name": "Concierto de Rock","date": "2027-01-15","capacity": 100,"price": 5000}                               |                 - Configurar una petición HTTP POST hacia el endpoint /events.<br />- Agregar el token de autenticación en el Header.<br />- Incluir en el body datos válidos para la creación del evento.<br />Enviar la petición.                 |                   Código de estado 200 OK.<br />El sistema registra el nuevo evento en el archivo events.json.<br />El sistema asigna automáticamente el valor `"owner_username": "organizer1"` al evento creado.<br />El sistema establece los valores iniciales por defecto correspondientes.<br />La respuesta contiene la entidad completa del evento recién creado.                    |
| **CP_16** |             Datos inconsistentes según esquema estructural              |                                                                      • Existe un usuario autenticado con un token válido.<br/> • El endpoint /events está operativo.                                                                      |                              **Authorization (Header):**<br/>token-organizer1<br />**Body:**<br />{"name": "Concierto de Rock","date": "2027-01-15","capacity": -10,"price": 5000}                              |               - Configurar una petición HTTP POST hacia el endpoint /events.<br />- Agregar el token de autenticación en el Header.<br />- Incluir en el body un valor inválido para el atributo capacity.<br />- Enviar la petición.               | Código de estado 422 Unprocessable Content.<br />La capa de validación detecta que el valor de capacity no cumple con las restricciones del modelo.<br />El sistema rechaza la solicitud antes de ejecutar la lógica de creación del evento.<br />No se agrega ningún registro nuevo al archivo events.json.<br />La respuesta contiene el detalle del error de validación correspondiente. |
| **CP_17** |                   Consulta exitosa de eventos activos                   |                                                         • Existen múltiples eventos activos registrados en el archivo `events.json`.  <br>• El endpoint `/events` está operativo.                                                         |                                                                     **Método:** GET  <br>**Endpoint:** `/events`  <br>**Headers:** Ninguno                                                                      |                                                                              -Configurar una petición HTTP GET hacia el endpoint `/events`.  <br>-Enviar la petición.                                                                               |                                                                                                  • El sistema responde con código de estado **200 OK**.  <br>• Se devuelve una colección con los eventos activos registrados.  <br>• La respuesta posee el formato esperado según la API.                                                                                                   |
| **CP_18** |               Eventos eliminados lógicamente no visibles                |                                    • Existen eventos registrados en el archivo `events.json`.<br/>• Al menos un evento posee el atributo `deleted: true`.<br/>• El endpoint `/events` está operativo.                                     |                                                                      **Método:** GET<br/>**Endpoint:** `/events`<br/>**Headers:** Ninguno                                                                       |                                                                               -Configurar una petición HTTP GET hacia el endpoint `/events`. <br>-Enviar la petición.                                                                               |                                                                                       • El sistema responde con código de estado **200 OK**.  <br>• La colección de eventos no incluye aquellos con `deleted: true`.  <br>• Solo se devuelven los eventos activos en el formato esperado por la API.                                                                                        |
| **CP_19** |                       No existen eventos activos                        |                                                                          • No existen eventos activos registrados.  <br>• El endpoint `/events` está operativo.                                                                           |                                                                      **Método:** GET<br/>**Endpoint:** `/events`<br/>**Headers:** Ninguno                                                                       |                                                                               -Configurar una petición HTTP GET hacia el endpoint `/events`. <br>-Enviar la petición.                                                                               |                                                                                                             • El sistema responde con código de estado **200 OK**.  <br>• Se devuelve una colección vacía (`[]`).  <br>• La respuesta mantiene el formato esperado por la API.                                                                                                              |
| **CP_20** |                  Consulta exitosa de un evento por ID                   |                                                                   • Existe un evento con ID `123` registrado y activo.<br>• El endpoint `/events/{id}` está operativo.                                                                    |                                                                    **Método:** GET<br/>**Endpoint:** `/events/123`<br/>**Headers:** Ninguno                                                                     |                                                                            • Configurar una petición HTTP GET hacia el endpoint `/events/123`.<br>• Enviar la petición.                                                                             |                                                                                              • El sistema responde con código de estado **200 OK**.<br>• Se devuelve la información completa del evento correspondiente al ID `123`.<br>• La respuesta posee el formato esperado según la API.                                                                                              |
| **CP_21** |                     Consulta de evento inexistente                      |                                                                      • El endpoint `/events/{id}` está operativo.<br>• No existe un evento registrado con ID `9999`.                                                                      |                                                                    **Método:** GET<br/>**Endpoint:** `/events/999`<br/>**Headers:** Ninguno                                                                     |                                                                            • Configurar una petición HTTP GET hacia el endpoint `/events/9999`.<br>• Enviar la petición.                                                                            |                                                                                                                                 • El sistema responde con código de estado **404 Not Found**.<br>• Se devuelve el mensaje `{"detail": "Event not found"}`.                                                                                                                                  |
| **CP_22** |                Consulta de evento eliminado lógicamente                 |                                                       • Existe un evento con ID `123` registrado con el atributo `deleted: true`.<br/>• El endpoint `/events/{id}` está operativo.                                                        |                                                                     **Método:** GET<br>**Endpoint:** `/events/123`<br>**Headers:** Ninguno                                                                      |                                                                            • Configurar una petición HTTP GET hacia el endpoint `/events/123`.<br>• Enviar la petición.                                                                             |                                                                                    • El sistema responde con código de estado **404 Not Found**.<br>• El evento no es expuesto debido a que fue eliminado lógicamente.<br>• Se devuelve el mensaje correspondiente de recurso inexistente según la API.                                                                                     |
| **CP_23** |                     Actualización exitosa de evento                     |                           • Existe un evento con ID `123` registrado en `events.json`.<br>• El usuario está autenticado como dueño del evento o administrador.<br>• El endpoint `/events/{id}` está operativo.                            |   **Método:** PUT<br>**Endpoint:** `/events/123`<br>**Headers:** token-organizer1<br>**Body:**<br>{"name": "Rocky-Kong Fest",<br>"date": "2025-4-19",<br>"capacity": 13,<br>"price": 12,<br>"active": false}    |                                       • Configurar una petición HTTP PUT hacia `/events/123`.<br>• Incluir token de autenticación en el header.• Enviar un body con datos válidos.<br>• Ejecutar la petición.                                       |                                                                                                          • El sistema responde con código de estado **200 OK**.<br>• Se actualiza la información del evento en `events.json`.<br>• Se devuelve la entidad actualizada del evento.                                                                                                           |
| **CP_24** |            Usuario sin permisos para actualización de evento            |                             • Existe un evento con ID `123` perteneciente a otro organizador.<br>• El usuario autenticado no tiene permisos sobre el recurso.<br>• El endpoint `/events/{id}` está operativo.                             |     **Método:** PUT<br>**Endpoint:** `/events/123`<br>**Headers:** token-organizer2<br>**Body:** {"name": "Rocky-Kong Fest",<br>"date": "2025-4-19",<br>"capacity": 13,<br>"price": 12,<br>"active": false}     |                                     • Configurar una petición HTTP PUT hacia `/events/123`.<br>• Incluir token de autenticación en el header.<br>• Enviar la petición.<br>• Intentar modificar un evento ajeno.                                     |                                                                                                                • El sistema responde con código de estado **403 Forbidden**.<br>• La operación es rechazada por falta de permisos.<br>• No se realizan cambios en el evento.                                                                                                                |
| **CP_25** |               Actualización rechazada por datos inválidos               |                                      • Existe un evento con ID `123` registrado.<br>• El usuario está autenticado y tiene permisos sobre el evento.<br>• El endpoint `/events/{id}` está operativo.                                       | **Método:** PUT<br>**Endpoint:** `/events/123`<br>**Headers:** token-organizer1<br>**Body:** {"name": "Rocky-Kong Fest",<br>"date": "xD",<br>"capacity": -2,<br>"price": 12,<br>"active": "Esta es una prueba"} |                                    • Configurar una petición HTTP PUT hacia `/events/123`.<br>• Incluir token de autenticación en el header.<br>• Enviar un body con datos inválidos.<br>• Ejecutar la petición.                                    |                                                                                                         • El sistema responde con código de estado **422 Unprocessable Content**.<br>• La validación de esquema rechaza la solicitud.<br>• No se modifica el evento en el sistema.                                                                                                          |
| **CP_26** |                   Evento inexistente en actualización                   |                                                                      • No existe un evento registrado con ID `9999`.<br>• El endpoint `/events/{id}` está operativo.                                                                      |    **Método:** PUT<br>**Endpoint:** `/events/9999`<br>**Headers:** token-organizer1<br>**Body:** {"name": "Rocky-Kong Fest",<br>"date": "2025-4-19",<br>"capacity": 13,<br>"price": 12,<br>"active": false}     |                                                         • Configurar una petición HTTP PUT hacia `/events/9999`.<br>• Incluir token de autenticación en el header.<br>• Enviar la petición.                                                         |                                                                                                                    • El sistema responde con código de estado **404 Not Found**.<br>• No se encuentra el recurso solicitado.<br>• No se realizan cambios en el sistema.                                                                                                                     |
| **CP_27** |                     Desactivación exitosa de evento                     |                                • Existe un evento activo con ID `123`.<br>• El usuario está autenticado como dueño del evento o administrador.<br>• El endpoint `/events/{id}/deactivate` está operativo.                                 |                                                          **Método:** PATCH<br>**Endpoint:** `/events/123/deactivate`<br>**Headers:** token-organizer1                                                           |                                                   • Configurar una petición HTTP PATCH hacia `/events/123/deactivate`.<br>• Incluir token de autenticación en el header.<br>• Enviar la petición.                                                   |                                                                                                                        • El sistema responde con código de estado **200 OK**.<br>• El evento se marca como inactivo.<br>• El cambio queda persistido en el sistema.                                                                                                                         |
| **CP_28** |               Usuario sin permisos para desactivar evento               |                       • Existe un evento con ID `123` perteneciente a otro organizador.<br>• El usuario autenticado no tiene permisos sobre el recurso.<br>• El endpoint `/events/{id}/deactivate` está operativo.                        |                                                          **Método:** PATCH<br>**Endpoint:** `/events/123/deactivate`<br>**Headers:** token-organizer2                                                           |                                                   • Configurar una petición HTTP PATCH hacia `/events/123/deactivate`.<br>• Incluir token de autenticación en el header.<br>• Enviar la petición.                                                   |                                                                                                                     • El sistema responde con código de estado **403 Forbidden**.<br>• La operación es rechazada por falta de permisos.<br>• El evento no se modifica.                                                                                                                      |
| **CP_29** |                   Evento inexistente en desactivación                   |                                                                • No existe un evento registrado con ID `9999`.<br>• El endpoint `/events/{id}/deactivate` está operativo.                                                                 |                                                          **Método:** PATCH<br>**Endpoint:** `/events/9999/deactivate`<br>**Headers:** token-organizer1                                                          |                                                  • Configurar una petición HTTP PATCH hacia `/events/9999/deactivate`.<br>• Incluir token de autenticación en el header.<br>• Enviar la petición.                                                   |                                                                                                                        • El sistema responde con código de estado **404 Not Found**.<br>• El recurso solicitado no existe.<br>• No se realiza ninguna modificación.                                                                                                                         |
| **CP_30** |                   Desactivación de evento ya inactivo                   |                           • Existe un evento previamente desactivado (estado inactivo).<br>• El usuario está autenticado como dueño o administrador.<br>• El endpoint `/events/{id}/deactivate` está operativo.                           |                                                          **Método:** PATCH<br>**Endpoint:** `/events/123/deactivate`<br>**Headers:** token-organizer1                                                           |                                         • Configurar una petición HTTP PATCH hacia `/events/123/deactivate`.• Incluir token de autenticación en el header.• Enviar la petición sobre un evento ya inactivo.                                         |                                                                                                                   • El sistema responde con código de estado 200 OK.<br>• El evento permanece como inactivo.<br>• El cambio queda persistido en el archivo `events.json`                                                                                                                    |
| **CP_31** |                       Consulta exitosa de órdenes                       |                                                  • Existen órdenes registradas en el sistema.<br>• El usuario está autenticado correctamente.<br>• El endpoint `/orders` está operativo.                                                  |                                                                   **Método:** GET<br>**Endpoint:** `/orders`<br>**Headers:** token-customer1                                                                    |                                                           • Configurar una petición HTTP GET hacia `/orders`.<br>• Incluir token de autenticación en el header.<br>• Enviar la petición.                                                            |                                                                                                    • El sistema responde con código de estado **200 OK**.<br>• Se devuelven las órdenes disponibles según el rol del usuario.<br>• La respuesta mantiene el formato esperado por la API.                                                                                                    |
| **CP_32** |              Error de autenticación por token inexistente               |                                                                  • El endpoint `/orders` está operativo.<br>• No se envía token de autenticación o el token es inválido.                                                                  |                                                                 **Método:** POST<br>**Endpoint:** `/orders`<br>**Headers:** token-customer9999                                                                  |                                                 • Configurar una petición HTTP hacia un endpoint protegido.<br>• No incluir token de autenticación o enviar uno inválido.<br>• Enviar la solicitud.                                                 |                                                                                                            • El sistema responde con código de estado **401 Unauthorized**.<br>• Se rechaza la solicitud por falta de autenticación.<br>• No se expone información del recurso.                                                                                                             |
| **CP_33** |                    Consulta de órdenes sin registros                    |                                               • El usuario está autenticado correctamente.<br>• No existen órdenes registradas para el usuario.<br>• El endpoint `/orders` está operativo.                                                |                                                                   **Método:** GET<br>**Endpoint:** `/orders`<br>**Headers:** token-customer2                                                                    |                                                                 • Configurar una petición HTTP GET hacia `/orders`.<br>• Incluir token de autenticación.<br>• Enviar la solicitud.                                                                  |                                                                                                                 • El sistema responde con código de estado **200 OK**.<br>• Se devuelve una lista vacía (`[]`).<br>• La respuesta mantiene el formato esperado por la API.                                                                                                                  |
| **CP_34** |                       Compra exitosa de entradas                        |                                            • Existe un evento activo con disponibilidad suficiente.<br>• El usuario está autenticado como cliente.<br>• El endpoint `/orders` está operativo.                                             |                                            **Método:** POST<br>**Endpoint:** `/orders`<br>**Headers:** token-customer1<br>**Body:**<br>`{"event_id":1,"quantity":2}`                                            |                             • Configurar una petición HTTP POST hacia `/orders`.<br>• Incluir token de autenticación en el header.• Enviar un body con la cantidad de entradas solicitadas.<br>• Ejecutar la petición.                              |                                                                                                       • El sistema responde con código de estado **200 OK**.<br>• Se registra la orden en `orders.json`.<br>• Se confirma la compra en la respuesta con los datos de la orden creada.                                                                                                       |
| **CP_35** |               Error de negocio por capacidad insuficiente               |                          • Existe un evento activo.<br>• La cantidad de entradas disponibles es menor a la solicitada.<br>• El usuario está autenticado como cliente.<br>• El endpoint `/orders` está operativo.                          |                                            **Método:** POST<br>**Endpoint:** `/orders`<br>**Headers:** token-customer1<br>**Body:** `{"event_id":1,"quantity":2000}`                                            |                             • Configurar una petición HTTP POST hacia `/orders`.<br>• Incluir token de autenticación en el header.• Enviar una cantidad de entradas mayor a la disponible.<br>• Ejecutar la solicitud.                              |                                                                                                                    • El sistema responde con código de estado **400 Bad Request**.<br>• Se rechaza la compra por falta de disponibilidad.<br>• No se crea ninguna orden.                                                                                                                    |
| **CP_36** |              Error de ciclo de vida en compra de entradas               |                                             • Existe un evento desactivado o eliminado lógicamente.<br>• El usuario está autenticado como cliente.<br>• El endpoint `/orders` está operativo.                                             |                                          **Método:** POST<br>**Endpoint:** `/orders`<br>**Headers:** token-customer1<br>**Body:** <br>`{"event_id":1234,"quantity":5}`                                          |                               • Configurar una petición HTTP POST hacia `/orders`.<br>• Incluir token de autenticación en el header.<br>• Intentar comprar entradas de un evento inactivo.<br>• Enviar la solicitud.                                |                                                                                                               • El sistema responde con código de estado **400 Bad Request**.<br>• Se rechaza la operación por estado inválido del evento.<br>• No se registra ninguna orden.                                                                                                               |
| **CP_37** |               Error de validación en cantidad de entradas               |                                              • El usuario está autenticado como cliente.<br>• El endpoint `/orders` está operativo.<br>• No se requiere existencia previa de disponibilidad.                                              |                                              **Método:** POST<br>**Endpoint:** `/orders`<br>**Headers:** token-customer1<br>**Body:** {"event_id":1,"quantity":0}                                               |                                    • Configurar una petición HTTP POST hacia `/orders`.<br>• Incluir token de autenticación en el header.<br>• Enviar una cantidad de entradas de -1.<br>• Ejecutar la petición.                                    |                                                                                                               • El sistema responde con código de estado **422 Unprocessable Content**.<br>• La validación de esquema rechaza la solicitud.<br>• No se procesa ninguna orden.                                                                                                               |
| **CP_38** |                      Cancelación exitosa de orden                       |                                       • Existe una orden activa con ID `10`.<br>• El usuario está autenticado como propietario de la orden.<br>• El endpoint `/orders/{id}/cancel` está operativo.                                        |                                                             **Método:** PATCH<br>**Endpoint:** `/orders/10/cancel`<br>**Headers:** token-customer1                                                              |                                • Configurar una petición HTTP PATCH hacia `/orders/10/cancel`.<br>• Incluir token de autenticación en el header.<br>• Enviar la solicitud.<br>• Ejecutar la cancelación de la orden.                                |                                                                                                            • El sistema responde con código de estado **200 OK**.<br>• El estado de la orden se actualiza a `"cancelled"`.<br>• La modificación queda persistida en el sistema.                                                                                                             |
| **CP_39** |                  Usuario ajeno intenta cancelar orden                   |                             • Existe una orden con ID `10` perteneciente a otro usuario.<br>• El usuario autenticado no es el propietario de la orden.<br>• El endpoint `/orders/{id}/cancel` está operativo.                             |                                                             **Método:** PATCH<br>**Endpoint:** `/orders/10/cancel`<br>**Headers:** token-customer2                                                              |                                        • Configurar una petición HTTP PATCH hacia `/orders/10/cancel`.<br>• Incluir token de autenticación de un usuario distinto al propietario.<br>• Enviar la solicitud.                                         |                                                                                                               • El sistema responde con código de estado **403 Forbidden**.<br>• La operación es rechazada por falta de permisos.<br>• No se modifica el estado de la orden.                                                                                                                |
| **CP_40** |                    Cancelación de orden inexistente                     |                                                                        • No existe una orden con ID `9999`.<br>• El endpoint `/orders/{id}/cancel` está operativo.                                                                        |                                                            **Método:** PATCH<br>**Endpoint:** `/orders/9999/cancel`<br>**Headers:** token-customer1                                                             |                                                    • Configurar una petición HTTP PATCH hacia `/orders/9999/cancel`.<br>• Incluir token de autenticación en el header.<br>• Enviar la solicitud.                                                    |                                                                                                   • El sistema responde con código de estado **404 Not Found**.<br>• Se devuelve el mensaje `{"detail": "Orden no encontrada."}`.<br>• No se realiza ninguna modificación en el sistema.                                                                                                    |
| **CP_41** |                    Cancelación de orden ya cancelada                    |               • Existe una orden con ID `10`.<br>• La orden ya se encuentra en estado `"cancelled"`.<br>• El usuario está autenticado como propietario de la orden.<br>• El endpoint `/orders/{id}/cancel` está operativo.                |                                                             **Método:** PATCH<br>**Endpoint:** `/orders/10/cancel`<br>**Headers:** token-customer1                                                              |                        • Configurar una petición HTTP PATCH hacia `/orders/10/cancel`.<br>• Incluir token de autenticación en el header.<br>• Intentar cancelar nuevamente una orden ya cancelada.<br>• Enviar la solicitud.                        |                                                                                                     • El sistema responde con código de estado **400 Bad Request**.<br>• Se rechaza la operación por estado inválido de la orden.<br>• No se realizan cambios adicionales en la orden.                                                                                                      |
#### **Matriz de trazabilidad**

| **ID de Requisito** | **Descripción**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **ID del Caso de Prueba** | **Descripción del caso de prueba**                                                                                                                                                                                                                                                                                                                                                                                                                             | **Estado**     |
| :------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| **REQ_01**          | Permite que un administrador o el organizador propietario de un evento pueda modificar la capacidad total disponible del mismo. El sistema debe validar que la nueva capacidad sea mayor a cero, que no sea menor a la cantidad de entradas ya vendidas y que el evento no se encuentre eliminado o inactivo. Además, solo usuarios autorizados podrán realizar esta acción.                                                                                                                                                                                                                                                                                                                                                                    | **CP_01**                 | Este caso de prueba verifica que un usuario autorizado pueda modificar correctamente la capacidad máxima de un evento existente mediante una petición PATCH. El sistema debe aceptar el cambio, actualizar la capacidad del evento y devolver el evento ya modificado con el nuevo valor guardado.                                                                                                                                                             | Aprobado       |
| **REQ_01**          | Permite que un administrador o el organizador propietario de un evento pueda modificar la capacidad total disponible del mismo. El sistema debe validar que la nueva capacidad sea mayor a cero, que no sea menor a la cantidad de entradas ya vendidas y que el evento no se encuentre eliminado o inactivo. Además, solo usuarios autorizados podrán realizar esta acción.                                                                                                                                                                                                                                                                                                                                                                    | **CP_02**                 | Este caso de prueba verifica que el sistema impida reducir la capacidad de un evento a un valor menor que la cantidad de entradas ya vendidas. El sistema debe rechazar la modificación, no actualizar la información del evento y devolver un error indicando que la nueva capacidad no puede ser inferior a las entradas vendidas.                                                                                                                           | Aprobado       |
| **REQ_01**          | Permite que un administrador o el organizador propietario de un evento pueda modificar la capacidad total disponible del mismo. El sistema debe validar que la nueva capacidad sea mayor a cero, que no sea menor a la cantidad de entradas ya vendidas y que el evento no se encuentre eliminado o inactivo. Además, solo usuarios autorizados podrán realizar esta acción.                                                                                                                                                                                                                                                                                                                                                                    | **CP_03**                 | Este caso de prueba verifica que el sistema valide correctamente que la capacidad de un evento no pueda establecerse en cero o en un valor negativo. El sistema debe rechazar la solicitud, mantener la capacidad actual sin cambios y devolver un error indicando que la capacidad debe ser mayor a cero.                                                                                                                                                     | ***Validado*** |
| **REQ_01**          | Permite que un administrador o el organizador propietario de un evento pueda modificar la capacidad total disponible del mismo. El sistema debe validar que la nueva capacidad sea mayor a cero, que no sea menor a la cantidad de entradas ya vendidas y que el evento no se encuentre eliminado o inactivo. Además, solo usuarios autorizados podrán realizar esta acción.                                                                                                                                                                                                                                                                                                                                                                    | **CP_04**                 | Este caso de prueba verifica que el sistema no permite modificar la capacidad de un evento que fue eliminado lógicamente o que se encuentra inactivo. El sistema debe rechazar la operación y devolver un error indicando que el evento no está disponible para ser modificado.                                                                                                                                                                                | Aprobado       |
| **REQ_01**          | Permite que un administrador o el organizador propietario de un evento pueda modificar la capacidad total disponible del mismo. El sistema debe validar que la nueva capacidad sea mayor a cero, que no sea menor a la cantidad de entradas ya vendidas y que el evento no se encuentre eliminado o inactivo. Además, solo usuarios autorizados podrán realizar esta acción.                                                                                                                                                                                                                                                                                                                                                                    | **CP_05**                 | Este caso de prueba verifica que el sistema impide modificar la capacidad de un evento cuando el usuario autenticado no es el propietario del evento ni posee permisos suficientes. El sistema debe rechazar la solicitud, no realizar cambios en el evento y devolver un error de autorización indicando que el usuario no tiene permisos para efectuar la modificación.                                                                                      | Aprobado       |
| **REQ_02**          | Este requerimiento permite que un administrador o el dueño de un evento puedan darlo de baja mediante una eliminación lógica, marcándolo como eliminado sin borrar la información físicamente del sistema. El administrador puede eliminar cualquier evento, mientras que el dueño solo podrá hacerlo si el evento no registra órdenes asociadas. El sistema también debe validar permisos y la existencia del evento antes de aplicar el cambio.                                                                                                                                                                                                                                                                                               | **CP_06**                 | Este caso de prueba verifica que un administrador pueda realizar la eliminación lógica de un evento que posee órdenes asociadas. El sistema debe marcar el evento como eliminado en el archivo JSON, cambiando el atributo delete da verdadero, sin borrar físicamente la información del evento.                                                                                                                                                              | Aprobado       |
| **REQ_02**          | Este requerimiento permite que un administrador o el dueño de un evento puedan darlo de baja mediante una eliminación lógica, marcándolo como eliminado sin borrar la información físicamente del sistema. El administrador puede eliminar cualquier evento, mientras que el dueño solo podrá hacerlo si el evento no registra órdenes asociadas. El sistema también debe validar permisos y la existencia del evento antes de aplicar el cambio.                                                                                                                                                                                                                                                                                               | **CP_07**                 | Este caso de prueba verifica que el dueño de un evento pueda realizar correctamente la eliminación lógica de su propio evento. El sistema debe aceptar la solicitud, marcar el evento como eliminado en el archivo de datos y devolver una respuesta exitosa.                                                                                                                                                                                                  | Aprobado       |
| **REQ_02**          | Este requerimiento permite que un administrador o el dueño de un evento puedan darlo de baja mediante una eliminación lógica, marcándolo como eliminado sin borrar la información físicamente del sistema. El administrador puede eliminar cualquier evento, mientras que el dueño solo podrá hacerlo si el evento no registra órdenes asociadas. El sistema también debe validar permisos y la existencia del evento antes de aplicar el cambio.                                                                                                                                                                                                                                                                                               | **CP_08**                 | Este caso de prueba verifica que el dueño de un evento no pueda eliminarlo si ya existen órdenes registradas asociadas al mismo, incluso si dichas órdenes están canceladas. El sistema debe rechazar la eliminación, mantener el estado actual del evento y devolver un error indicando que no es posible eliminar eventos con órdenes registradas.                                                                                                           | ***Validado*** |
| **REQ_02**          | Este requerimiento permite que un administrador o el dueño de un evento puedan darlo de baja mediante una eliminación lógica, marcándolo como eliminado sin borrar la información físicamente del sistema. El administrador puede eliminar cualquier evento, mientras que el dueño solo podrá hacerlo si el evento no registra órdenes asociadas. El sistema también debe validar permisos y la existencia del evento antes de aplicar el cambio.                                                                                                                                                                                                                                                                                               | **CP_09**                 | Este caso de prueba verifica que un organizador que no es el dueño del evento no pueda eliminarlo. El sistema debe rechazar la solicitud de borrado y devolver un error indicando que el usuario no posee permisos suficientes para realizar la eliminación del evento.                                                                                                                                                                                        | Aprobado       |
| **REQ_02**          | Este requerimiento permite que un administrador o el dueño de un evento puedan darlo de baja mediante una eliminación lógica, marcándolo como eliminado sin borrar la información físicamente del sistema. El administrador puede eliminar cualquier evento, mientras que el dueño solo podrá hacerlo si el evento no registra órdenes                                                                                                                                                                                                                                                                                                                                                                                                          | **CP_10**                 | Este caso de prueba verifica que el sistema maneje correctamente la solicitud de eliminación de un evento inexistente. El sistema debe detectar que el ID solicitado no corresponde a ningún evento registrado y devolver un error indicando que el recurso no fue encontrado.                                                                                                                                                                                 | Aprobado       |
| **REQ_03**          | Este requerimiento permite que un visitante anónimo pueda registrarse en el sistema mediante el ingreso de un nombre de usuario y una contraseña, con el fin de crear una cuenta que le permita acceder a las funcionalidades de la plataforma. El sistema debe verificar que el nombre de usuario no exista previamente en el archivo de persistencia antes de completar el registro. Si el identificador se encuentra disponible, deberá crear el nuevo usuario y almacenarlo en el archivo users.json. En caso de que el nombre de usuario ya exista, el sistema deberá rechazar la solicitud sin realizar modificaciones en los datos almacenados e informar el error correspondiente.                                                      | **CP_11**                 | Este caso de prueba verifica que el sistema permita registrar correctamente un nuevo usuario cuando el nombre de usuario ingresado no existe previamente en el archivo de persistencia. El sistema debe crear el nuevo registro en el archivo users.json, almacenar los datos proporcionados y devolver una respuesta exitosa confirmando la creación de la cuenta.                                                                                            | Aprobado       |
| **REQ_03**          | Este requerimiento permite que un visitante anónimo pueda registrarse en el sistema mediante el ingreso de un nombre de usuario y una contraseña, con el fin de crear una cuenta que le permita acceder a las funcionalidades de la plataforma. El sistema debe verificar que el nombre de usuario no exista previamente en el archivo de persistencia antes de completar el registro. Si el identificador se encuentra disponible, deberá crear el nuevo usuario y almacenarlo en el archivo users.json. En caso de que el nombre de usuario ya exista, el sistema deberá rechazar la solicitud sin realizar modificaciones en los datos almacenados e informar el error correspondiente.                                                      | **CP_12**                 | Este caso de prueba verifica que el sistema impida el registro de un usuario cuando el nombre de usuario ingresado ya existe en el archivo de persistencia. El sistema debe rechazar la solicitud, mantener sin cambios el contenido del archivo users.json y devolver un error indicando que el usuario ya se encuentra registrado.                                                                                                                           | Aprobado       |
| **REQ_04**          | Este requerimiento permite que un usuario registrado pueda autenticarse en el sistema mediante el ingreso de sus credenciales de acceso. El sistema debe validar que el nombre de usuario y la contraseña coincidan con los registros almacenados en el archivo users.json. Si las credenciales son correctas, deberá generar y devolver un token de acceso que permita al usuario identificarse en solicitudes posteriores. En caso de que los datos ingresados sean inválidos, el sistema deberá rechazar la autenticación y denegar la entrega del token correspondiente.                                                                                                                                                                    | **CP_13**                 | Este caso de prueba verifica que un usuario registrado pueda iniciar sesión correctamente utilizando credenciales válidas. El sistema debe validar la coincidencia entre el nombre de usuario y la contraseña almacenados en el archivo users.json, generar un token de acceso y devolver una respuesta exitosa que contenga la estructura del access_token.                                                                                                   | Aprobado       |
| **REQ_04**          | Este requerimiento permite que un usuario registrado pueda autenticarse en el sistema mediante el ingreso de sus credenciales de acceso. El sistema debe validar que el nombre de usuario y la contraseña coincidan con los registros almacenados en el archivo users.json. Si las credenciales son correctas, deberá generar y devolver un token de acceso que permita al usuario identificarse en solicitudes posteriores. En caso de que los datos ingresados sean inválidos, el sistema deberá rechazar la autenticación y denegar la entrega del token correspondiente.                                                                                                                                                                    | **CP_14**                 | Este caso de prueba verifica que el sistema rechace correctamente un intento de autenticación cuando las credenciales ingresadas no coinciden con las almacenadas en el archivo users.json. El sistema debe bloquear el proceso de inicio de sesión, no generar ningún token de acceso y devolver un error indicando que las credenciales proporcionadas son inválidas.                                                                                        | Aprobado       |
| **REQ_05**          | Este requerimiento permite que un usuario autenticado pueda crear un nuevo evento dentro de la plataforma mediante el ingreso de los datos necesarios para su configuración inicial. El sistema debe validar que la información proporcionada cumpla con las restricciones definidas por el modelo de datos, incluyendo una fecha válida, una capacidad mayor a cero y un precio positivo. Si los datos son correctos, deberá registrar el evento en el archivo events.json, asociarlo al usuario creador mediante el atributo owner_username y devolver la entidad creada. En caso de que los datos enviados no cumplan con el esquema establecido, el sistema deberá rechazar la solicitud y devolver el error de validación correspondiente. | **CP_15**                 | Este caso de prueba verifica que un usuario autenticado pueda crear correctamente un nuevo evento utilizando datos válidos. El sistema debe registrar el evento en el archivo events.json, asociarlo automáticamente al usuario creador mediante el atributo owner_username, establecer los valores iniciales correspondientes y devolver una respuesta exitosa que contenga la entidad completa del evento creado.                                            | Aprobado       |
| **REQ_05**          | Este requerimiento permite que un usuario autenticado pueda crear un nuevo evento dentro de la plataforma mediante el ingreso de los datos necesarios para su configuración inicial. El sistema debe validar que la información proporcionada cumpla con las restricciones definidas por el modelo de datos, incluyendo una fecha válida, una capacidad mayor a cero y un precio positivo. Si los datos son correctos, deberá registrar el evento en el archivo events.json, asociarlo al usuario creador mediante el atributo owner_username y devolver la entidad creada. En caso de que los datos enviados no cumplan con el esquema establecido, el sistema deberá rechazar la solicitud y devolver el error de validación correspondiente. | **CP_16**                 | Este caso de prueba verifica que el sistema valide correctamente la estructura y las restricciones de los datos recibidos durante la creación de un evento. Cuando se envía un valor inválido para el atributo capacity, el sistema debe detectar el error durante la etapa de validación, rechazar la solicitud antes de ejecutar la lógica de negocio y devolver una respuesta con código 422 Unprocessable Content informando la inconsistencia encontrada. | Aprobado       |
| **REQ_06**          | Este requerimiento permite consultar todos los eventos activos registrados en el sistema. El sistema deberá excluir aquellos eventos que hayan sido eliminados lógicamente y responder correctamente incluso cuando no existan eventos disponibles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **CP_17**                 | Este caso de prueba verifica que el sistema permita recuperar correctamente todos los eventos activos registrados mediante una petición GET, devolviendo una colección con los eventos disponibles.                                                                                                                                                                                                                                                            | Aprobado       |
| **REQ_06**          | Este requerimiento permite consultar todos los eventos activos registrados en el sistema. El sistema deberá excluir aquellos eventos que hayan sido eliminados lógicamente y responder correctamente incluso cuando no existan eventos disponibles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **CP_18**                 | Este caso de prueba verifica que el sistema filtre correctamente los eventos eliminados lógicamente, impidiendo que sean visibles dentro de la respuesta devuelta al consultar el listado general de eventos.                                                                                                                                                                                                                                                  | Aprobado       |
| **REQ_06**          | Este requerimiento permite consultar todos los eventos activos registrados en el sistema. El sistema deberá excluir aquellos eventos que hayan sido eliminados lógicamente y responder correctamente incluso cuando no existan eventos disponibles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **CP_19**                 | Este caso de prueba verifica que el sistema responda correctamente cuando no existen eventos activos registrados, devolviendo una lista vacía sin generar errores.                                                                                                                                                                                                                                                                                             | Aprobado       |
| **REQ_07**          | Este requerimiento permite consultar el detalle de un evento específico mediante su identificador único. El sistema deberá validar que el evento exista y que no se encuentre eliminado lógicamente antes de devolver la información solicitada.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **CP_20**                 | Este caso de prueba verifica que el sistema permita consultar correctamente un evento existente mediante su identificador, devolviendo la entidad correspondiente y una respuesta exitosa.                                                                                                                                                                                                                                                                     | Aprobado       |
| **REQ_07**          | Este requerimiento permite consultar el detalle de un evento específico mediante su identificador único. El sistema deberá validar que el evento exista y que no se encuentre eliminado lógicamente antes de devolver la información solicitada.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **CP_21**                 | Este caso de prueba verifica que el sistema detecte correctamente cuando se solicita un evento inexistente, devolviendo un error indicando que el recurso no fue encontrado.                                                                                                                                                                                                                                                                                   | Aprobado       |
| **REQ_07**          | Este requerimiento permite consultar el detalle de un evento específico mediante su identificador único. El sistema deberá validar que el evento exista y que no se encuentre eliminado lógicamente antes de devolver la información solicitada.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **CP_22**                 | Este caso de prueba verifica que el sistema impida consultar eventos que fueron eliminados lógicamente, devolviendo un error equivalente a recurso inexistente.                                                                                                                                                                                                                                                                                                | Aprobado       |
| **REQ_08**          | Este requerimiento permite modificar la información general de un evento existente. Solo podrán realizar esta acción el administrador o el usuario propietario del evento. El sistema deberá validar permisos, existencia del recurso y consistencia de los datos enviados.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **CP_23**                 | Este caso de prueba verifica que un usuario autorizado pueda modificar correctamente un evento existente enviando datos válidos, actualizando la información persistida y devolviendo el evento actualizado.                                                                                                                                                                                                                                                   | Aprobado       |
| **REQ_08**          | Este requerimiento permite modificar la información general de un evento existente. Solo podrán realizar esta acción el administrador o el usuario propietario del evento. El sistema deberá validar permisos, existencia del recurso y consistencia de los datos enviados.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **CP_24**                 | Este caso de prueba verifica que el sistema rechace correctamente la modificación de un evento cuando el usuario autenticado no posee permisos suficientes sobre dicho recurso.                                                                                                                                                                                                                                                                                | Aprobado       |
| **REQ_08**          | Este requerimiento permite modificar la información general de un evento existente. Solo podrán realizar esta acción el administrador o el usuario propietario del evento. El sistema deberá validar permisos, existencia del recurso y consistencia de los datos enviados.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **CP_25**                 | Este caso de prueba verifica que el sistema valide correctamente los datos enviados durante una actualización, rechazando valores inconsistentes o inválidos según el modelo establecido.                                                                                                                                                                                                                                                                      | Aprobado       |
| **REQ_08**          | Este requerimiento permite modificar la información general de un evento existente. Solo podrán realizar esta acción el administrador o el usuario propietario del evento. El sistema deberá validar permisos, existencia del recurso y consistencia de los datos enviados.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **CP_26**                 | Este caso de prueba verifica que el sistema detecte correctamente la actualización sobre un evento inexistente, devolviendo un error indicando que el recurso solicitado no existe.                                                                                                                                                                                                                                                                            | Aprobado       |
| **REQ_09**          | Este requerimiento permite desactivar eventos activos sin eliminarlos del sistema. La acción podrá ser ejecutada únicamente por el administrador o el propietario del evento, validando permisos, existencia y estado actual del recurso.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **CP_27**                 | Este caso de prueba verifica que un usuario autorizado pueda desactivar correctamente un evento activo existente, modificando su estado interno y devolviendo una respuesta exitosa.                                                                                                                                                                                                                                                                           | Aprobado       |
| **REQ_09**          | Este requerimiento permite desactivar eventos activos sin eliminarlos del sistema. La acción podrá ser ejecutada únicamente por el administrador o el propietario del evento, validando permisos, existencia y estado actual del recurso.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **CP_28**                 | Este caso de prueba verifica que el sistema rechace la desactivación cuando el usuario autenticado no posee permisos suficientes para operar sobre el evento solicitado.                                                                                                                                                                                                                                                                                       | Aprobado       |
| **REQ_09**          | Este requerimiento permite desactivar eventos activos sin eliminarlos del sistema. La acción podrá ser ejecutada únicamente por el administrador o el propietario del evento, validando permisos, existencia y estado actual del recurso.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **CP_29**                 | Este caso de prueba verifica que el sistema detecte correctamente cuando se intenta desactivar un evento inexistente, devolviendo el error correspondiente de recurso no encontrado.                                                                                                                                                                                                                                                                           | Aprobado       |
| **REQ_09**          | Este requerimiento permite desactivar eventos activos sin eliminarlos del sistema. La acción podrá ser ejecutada únicamente por el administrador o el propietario del evento, validando permisos, existencia y estado actual del recurso.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **CP_30**                 | Este caso de prueba verifica que el sistema impida desactivar nuevamente un evento que ya se encuentra previamente inactivo, devolviendo un error correspondiente al estado inválido del recurso.                                                                                                                                                                                                                                                              | Aprobado       |
| **REQ_10**          | Este requerimiento permite consultar las órdenes registradas dentro del sistema según el rol del usuario autenticado. El sistema deberá validar autenticación y responder correctamente incluso cuando no existan órdenes asociadas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_31**                 | Este caso de prueba verifica que el sistema permita consultar correctamente las órdenes registradas visibles según el rol del usuario autenticado, devolviendo la colección correspondiente.                                                                                                                                                                                                                                                                   | Aprobado       |
| **REQ_10**          | Este requerimiento permite consultar las órdenes registradas dentro del sistema según el rol del usuario autenticado. El sistema deberá validar autenticación y responder correctamente incluso cuando no existan órdenes asociadas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_32**                 | Este caso de prueba verifica que el sistema rechace correctamente una consulta de órdenes cuando no se proporciona un token de autenticación válido, devolviendo error de autenticación.                                                                                                                                                                                                                                                                       | Aprobado       |
| **REQ_10**          | Este requerimiento permite consultar las órdenes registradas dentro del sistema según el rol del usuario autenticado. El sistema deberá validar autenticación y responder correctamente incluso cuando no existan órdenes asociadas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_33**                 | Este caso de prueba verifica que el sistema responda correctamente cuando no existen órdenes registradas para el usuario, devolviendo una lista vacía sin producir errores.                                                                                                                                                                                                                                                                                    | Aprobado       |
| **REQ_11**          | Este requerimiento permite que un cliente autenticado pueda comprar entradas para eventos activos con disponibilidad suficiente. El sistema deberá validar stock disponible, estado del evento y consistencia de la cantidad solicitada antes de registrar la orden.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_34**                 | Este caso de prueba verifica que un cliente autenticado pueda comprar entradas exitosamente cuando existe disponibilidad suficiente, registrando correctamente la orden dentro del sistema.                                                                                                                                                                                                                                                                    | Aprobado       |
| **REQ_11**          | Este requerimiento permite que un cliente autenticado pueda comprar entradas para eventos activos con disponibilidad suficiente. El sistema deberá validar stock disponible, estado del evento y consistencia de la cantidad solicitada antes de registrar la orden.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_35**                 | Este caso de prueba verifica que el sistema rechace correctamente una compra cuando el evento no posee capacidad suficiente para cubrir la cantidad solicitada por el cliente.                                                                                                                                                                                                                                                                                 | Aprobado       |
| **REQ_11**          | Este requerimiento permite que un cliente autenticado pueda comprar entradas para eventos activos con disponibilidad suficiente. El sistema deberá validar stock disponible, estado del evento y consistencia de la cantidad solicitada antes de registrar la orden.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_36**                 | Este caso de prueba verifica que el sistema impida la compra de entradas cuando el evento se encuentra desactivado o eliminado lógicamente, rechazando la operación solicitada.                                                                                                                                                                                                                                                                                | Aprobado       |
| **REQ_11**          | Este requerimiento permite que un cliente autenticado pueda comprar entradas para eventos activos con disponibilidad suficiente. El sistema deberá validar stock disponible, estado del evento y consistencia de la cantidad solicitada antes de registrar la orden.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **CP_37**                 | Este caso de prueba verifica que el sistema valide correctamente la cantidad solicitada durante la compra, rechazando cantidades negativas o iguales a cero.                                                                                                                                                                                                                                                                                                   | Aprobado       |
| **REQ_12**          | Este requerimiento permite cancelar órdenes previamente registradas por parte del usuario propietario de la compra. El sistema deberá validar autorización, existencia de la orden y estado actual antes de modificar la persistencia.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **CP_38**                 | Este caso de prueba verifica que un usuario pueda cancelar exitosamente una orden activa propia, actualizando correctamente su estado interno a cancelado.                                                                                                                                                                                                                                                                                                     | Aprobado       |
| **REQ_12**          | Este requerimiento permite cancelar órdenes previamente registradas por parte del usuario propietario de la compra. El sistema deberá validar autorización, existencia de la orden y estado actual antes de modificar la persistencia.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **CP_39**                 | Este caso de prueba verifica que el sistema rechace correctamente el intento de cancelación realizado por un usuario que no es propietario de la orden seleccionada.                                                                                                                                                                                                                                                                                           | Aprobado       |
| **REQ_12**          | Este requerimiento permite cancelar órdenes previamente registradas por parte del usuario propietario de la compra. El sistema deberá validar autorización, existencia de la orden y estado actual antes de modificar la persistencia.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **CP_40**                 | Este caso de prueba verifica que el sistema detecte correctamente cuando se intenta cancelar una orden inexistente, devolviendo el error correspondiente de recurso no encontrado.                                                                                                                                                                                                                                                                             | Aprobado       |
| **REQ_12**          | Este requerimiento permite cancelar órdenes previamente registradas por parte del usuario propietario de la compra. El sistema deberá validar autorización, existencia de la orden y estado actual antes de modificar la persistencia.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **CP_41**                 | Este caso de prueba verifica que el sistema impida cancelar nuevamente una orden que ya posee estado cancelado, devolviendo un error correspondiente a una operación inválida.                                                                                                                                                                                                                                                                                 | Aprobado       |
#### **Ejecución de pruebas manuales y evidencia de ejecución**

En base a la ejecución de pruebas manuales, cuya evidencia se encuentra dentro de la carpeta "Evidencias_CP" se dio con los siguientes defectos:

#### **Defecto – CP_03 (BUG_01):** Validación incorrecta de capacidad igual a cero

- **Precondiciones:**

  - Tener acceso al entorno de pruebas de la API (Postman).
  - Contar con las credenciales de un usuario organizador.

  - Tener un evento previamente creado activo en el sistema.

- **Pasos para reproducir:** 
  - Iniciar sesión con el usuario organizador en Postman para obtener el token de autenticación.
  - Configurar una petición HTTP de tipo PATCH apuntando al endpoint de actualización del evento.
  - Modificar el cuerpo de la petición (JSON) asignando el valor `0` al campo de capacidad (`capacity`).
  - Enviar la petición.

- **Resultado Obtenido:** El sistema permite actualizar la capacidad de un evento a un valor igual a cero. Además, ante el intento de actualización, devuelve el mensaje de error: `{ "detail": "Capacity cannot be lower than sold tickets" }`, el cual no corresponde con la validación requerida.

- **Resultado Esperado:** El sistema debe rechazar la actualización si la capacidad es menor o igual a cero, bloqueando el valor cero y devolviendo un mensaje de error que indique explícitamente que la capacidad debe ser estrictamente mayor a cero.
- **Evidencia:** La evidencia se encuentra disponible en la carpeta "Evidencias_CP", en esta carpeta se encontraran todos los videos de la ejecución de cada caso de prueba mediante **Postman** y sus respectivos resultados.
- **Severidad:** **<u>Mayor</u>** ya que permite registrar datos inconsistentes que contradicen las reglas de negocio y los requisitos funcionales establecidos.
- **Prioridad:** **<u>Media</u>** Debe corregirse para asegurar la integridad de la lógica del sistema antes de pasar a producción, aunque no bloquee por completo el flujo de la aplicación.

#### **Defecto – CP_08 (BUG_02):** Eliminación incorrecta de eventos con órdenes registradas

- **Precondiciones:**

  - Tener acceso al entorno de pruebas de la API (Postman).

  - Contar con las credenciales de un usuario organizador.

  - Tener un evento activo creado que posea al menos una orden registrada con estado "cancelada".

- **Pasos para reproducir: **

  - Iniciar sesión con el usuario organizador en Postman.

  - Configurar una petición HTTP de tipo DELETE apuntando al endpoint del evento específico que tiene las órdenes canceladas asociadas.

  - Enviar la petición.

  - Verificar el estado del archivo JSON de persistencia o enviar una petición GET para comprobar si el campo `deleted` cambió a `true`.

- **Resultado Obtenido:** El sistema permite eliminar un evento activo que posee órdenes registradas canceladas. Devuelve un estado exitoso con el cuerpo: `{"id": 3, "name": "Jazz Night", "date": "2025-12-20", "capacity": 50, "price": 4500, "active": false, "deleted": true, "owner_username": "organizer1"}`, marcando el evento como eliminado en el archivo JSON de persistencia.
- **Resultado Esperado:** El sistema debe rechazar la eliminación, devolver un status code 400, informar mediante un mensaje de error que no es posible eliminar eventos con órdenes registradas y mantener sin modificaciones el archivo JSON de persistencia.
- **Evidencia:** La evidencia se encuentra disponible en la carpeta "Evidencias_CP", en esta carpeta se encontraran todos los videos de la ejecución de cada caso de prueba mediante **Postman** y sus respectivos resultados.
- **Severidad:** **<u>Crítica</u>** ya que viola una regla de negocio esencial, permitiendo el borrado de datos vinculados a transacciones y rompiendo la integridad de la persistencia.
- **Prioridad:** **<u>Alta</u>** ya que impacta directamente en la consistencia de los datos del sistema y debe resolverse de forma prioritaria para evitar pérdida de historial de órdenes.
### **Pruebas de regresión posteriores a la implementación y corrección de defectos**

Una vez finalizada la implementación de los nuevos requerimientos correspondientes a los endpoints de **actualización de capacidad de eventos** (`PATCH /events/{id}/capacity`) y **eliminación lógica de eventos** (`DELETE /events/{id}`), se llevó a cabo una instancia complementaria de **pruebas de regresión manuales** con el objetivo de verificar que la incorporación de estas nuevas funcionalidades no hubiera introducido efectos colaterales sobre componentes previamente desarrollados e integrados dentro del sistema.

Dado que ambos endpoints implementados interactúan directamente con entidades centrales del dominio, particularmente sobre la gestión y persistencia de datos asociados a eventos, autenticación de usuarios, validación de permisos según roles y consistencia de la información almacenada en archivos JSON de persistencia, resultó necesario volver a ejecutar verificaciones sobre funcionalidades existentes relacionadas de manera directa e indirecta con dichos componentes.

Durante esta etapa de regresión se realizaron nuevamente validaciones manuales sobre endpoints previamente aprobados pertenecientes a módulos ya implementados, principalmente aquellos asociados a:

- Registro e inicio de sesión de usuarios (`/auth/register` y `/auth/login`).
- Creación y consulta general de eventos (`POST /events`, `GET /events`, `GET /events/{id}`).
- Actualización general de eventos (`PUT /events/{id}`).
- Desactivación lógica de eventos (`PATCH /events/{id}/deactivate`).
- Consulta y gestión de órdenes (`GET /orders`, `POST /orders`, `PATCH /orders/{id}/cancel`).

El objetivo de esta revalidación fue confirmar que las modificaciones introducidas sobre la lógica de negocio relacionada con eventos no alteraran el comportamiento esperado de funcionalidades previamente estables, especialmente en aspectos vinculados a autorización, validaciones de integridad, manejo de estados internos del evento y persistencia consistente de la información.

Adicionalmente, durante la ejecución inicial de pruebas manuales fueron detectados defectos en los casos de prueba **CP_03** y **CP_08**, ambos asociados a comportamientos incorrectos dentro de los nuevos endpoints implementados. Posteriormente, una vez que dichos defectos fueron analizados y corregidos por el equipo de desarrollo, se procedió a realizar una nueva ejecución de validación específica sobre ambos casos de prueba con el objetivo de comprobar la correcta resolución de los errores reportados.

Tras verificar que las correcciones aplicadas solucionaban efectivamente los defectos identificados, se ejecutó nuevamente una ronda adicional de pruebas de regresión manuales sobre los endpoints vinculados al módulo de eventos y al resto de funcionalidades previamente desarrolladas, confirmando que la aplicación continuaba manteniendo un comportamiento estable y consistente luego de la incorporación de cambios y posterior corrección de incidencias.

Como resultado final de esta etapa, se concluye que las nuevas funcionalidades implementadas fueron integradas correctamente dentro del sistema, los defectos detectados fueron resueltos satisfactoriamente y no se observaron regresiones funcionales sobre endpoints previamente validados, manteniéndose la estabilidad general de la API luego de los cambios realizados.

Cabe destacar que la totalidad de la evidencia correspondiente tanto a la ejecución de los casos de prueba iniciales, las validaciones posteriores a la corrección de defectos detectados en **CP_03** y **CP_08**, así como también las pruebas de regresión manuales efectuadas sobre los 41 casos de prueba posteriormente, se encuentra disponible dentro de la carpeta **“Evidencias_CP”**, donde se adjuntan los registros audiovisuales de ejecución en formato **.mkv** para su correspondiente verificación.